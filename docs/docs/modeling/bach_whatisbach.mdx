---
id: bach_whatisbach
title: What is Bach? — Bach  documentation
hide_title: true
slug: /modeling/bach_whatisbach


---
<head>
    <meta name="description" content="Bach is a library for analysing data that is stored in a SQL database. Currently we only support Postgres, but we plan on supporting more databases. We’re using an interface that’s mostly compatible with pandas. All data storage and processing is handled by the database. As a result, local memory does not limit the size of the data that can be analysed with Bach." />
    <meta property="og:description" content="Bach is a library for analysing data that is stored in a SQL database. Currently we only support Postgres, but we plan on supporting more databases. We’re using an interface that’s mostly compatible with pandas. All data storage and processing is handled by the database. As a result, local memory does not limit the size of the data that can be analysed with Bach." />
</head>

export const toc = [
    {
        "value": "Get started with Bach",
        "id": "get-started-with-bach",
        "children": [],
        "level": 2
    }
];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/bach_whatisbach.html')} />
